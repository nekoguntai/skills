# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Note: the grade plugin writes runtime history to ~/.claude/grade-history/
# (user-scoped, outside the repo). No ignore pattern needed for it — it can't
# end up inside this repo unless someone manually puts it there.
